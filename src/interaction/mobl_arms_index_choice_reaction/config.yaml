# 原有配置
dt: 0.01          # 仿真步长（调慢一点，看得清）
render_mode: "human"  # 开启可视化！

target_button: 0        # 目标按钮（可选0/1/2/3，对应红/绿/蓝/黄）
button_reward: 10.0     # 选对按钮的奖励值
effort_cost: 0.01       # 动作成本扣分系数（模型有执行器时生效）
timeout: 600            # 任务超时步数（300步≈9秒，可根据需要调整）